---
interface Props {
  href: string;
}

const { href } = Astro.props;
---

<a
  href={href}
  class="flex items-center lg:w-56 lg:py-5 gap-2 hover:from-blue-300 hover:to-emerald-300 bg-gradient-to-r from-blue-400 to-emerald-400 py-3 font-semibold justify-center px-6 rounded-lg"
  ><slot /></a
>
