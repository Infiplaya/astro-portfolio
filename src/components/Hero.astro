---
interface Props {
  title: string;
  tagline?: string;
  href?: string;
}

const { tagline, title, href } = Astro.props;
---

<header>
  <div class="text-center lg:text-left mt-3">
    <h1 class="text-4xl lg:text-5xl font-bold tracking-tighter">{title}</h1>
    {
      tagline && (
        <p class="text-slate-700 xl:pt-2 indent-4 tracking-normal leading-7 mt-3 dark:text-slate-300">
          {tagline}
          {href && (
            <a
              href={href}
              class="font-bold text-orange-600 dark:text-orange-400 dark:hover:text-orange-300 hover:text-orange-500"
            >
              Check about me
            </a>
          )}
        </p>
      )
    }
  </div>
  <slot />
</header>
